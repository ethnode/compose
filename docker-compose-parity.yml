version: "2"

services:
  ethereum:
    image: ethnode/parity:1.3.8
    volumes:
      - /data/store/parity:/root/.parity
  netstats:
    image: ethnode/stats
    environment:
      instance: 
      contact_details: 
    depends_on:
      - ethereum
